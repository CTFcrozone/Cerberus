[package]
name = "lib-event-schema"
version = "0.1.0"
edition.workspace = true


license.workspace = true

[dependencies]
regex = "1"

derive_more = { version = "2.0.0", features = [
    "from",
    "display",
    "debug",
    "into",
    "deref",
] }
